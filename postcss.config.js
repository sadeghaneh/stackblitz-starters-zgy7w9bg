import { withPayload } from '@payloadcms/next/withPayload'

/** @type {import('neexport default {
 *   plugins: {
     tailwindcss: {},
         autoprefixer: {},
           },
           }
           
