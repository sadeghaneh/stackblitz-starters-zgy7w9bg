import { withPayload } from '@payloadcms/next/withPayload'

/** @type {import('next').NextConfig} */
const nextConfig = {
  // تنظیمات دیگر نکست جی اس در صورت نیاز اینجا قرار می‌گیرند
  }

  export default withPayload(nextConfig)
  